import { createSlice } from "@reduxjs/toolkit";
const initialState = {
    
}
const tpoSlice = createSlice({
    name:'tpoSlice',
    initialState,
    reducers:{

    }
});

export const{} = tpoSlice.actions;
export default tpoSlice.reducer;